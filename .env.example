# 이 파일을 .env 로 복사한 뒤 실제 키를 채워 넣으세요.
# .env 는 gitignore 대상이므로 절대 커밋되지 않습니다.

# 경기도 버스정보(GBIS) API 키
GYEONGGI_BUS_API_KEY=your-gbis-api-key

# Google Maps API 키 (Android 는 이 값을 빌드 시 매니페스트에 주입)
# iOS 는 ios/Flutter/Secrets.xcconfig 에 같은 키를 설정해야 합니다.
MAPS_API_KEY=your-google-maps-api-key
